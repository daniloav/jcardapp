/**
 * Versão do app, exibida no login e no rodapé do menu lateral.
 *
 * Mantida à mão, junto com o `version` do `package.json`. Serve para o Danilo
 * saber, olhando a tela da pessoa, qual build ela está usando — no PWA o
 * service worker pode servir um shell antigo até a próxima visita.
 */
export const APP_VERSION = '1.0.0';

# Copie para .env e preencha. Este arquivo é o modelo; o .env real NÃO é
# versionado e, em produção, vem do secret DEPLOY_ENV_FILE (ver docs/CICD.md).

# ---------------------------------------------------------------- banco --
# Postgres no Neon (plano gratuito permanente). No painel do Neon, em
# "Connection Details", copie a connection string e quebre nas três partes:
#
#   postgresql://USUARIO:SENHA@ep-xxx-123456.us-east-2.aws.neon.tech/jcard?sslmode=require
#                └─user─┘ └sen┘ └──────────────── host ───────────────┘ └db┘
#
# ⚠️ O `?sslmode=require` é obrigatório — o Neon recusa conexão sem TLS.
JCARD_DB_URL=jdbc:postgresql://ep-xxx-123456.us-east-2.aws.neon.tech/jcard?sslmode=require
JCARD_DB_USER=jcard_owner
JCARD_DB_PASS=cole-a-senha-do-neon

# ------------------------------------------------------- primeiro acesso --
JCARD_ADMIN_NOME=Titular do cartao
JCARD_ADMIN_LOGIN=admin
JCARD_ADMIN_EMAIL=seu-email@gmail.com
JCARD_ADMIN_SENHA=troque-esta-senha
# Senha dada a cada utilizador novo; o app força a troca no 1º acesso.
JCARD_SENHA_PADRAO=12345678

# ------------------------------------------------------------ recebimento --
# A chave que o app mostra (com botão de copiar) na hora de pagar o acerto.
# CPF e nome completo são dado pessoal e o repositório é público: preencha aqui,
# no .env da VM (que não é versionado), e nunca no .env.example.
# Isto aqui é o valor INICIAL: assim que o admin salvar a chave na tela
# (/admin/pix) o banco passa a mandar, e mexer neste arquivo não muda mais nada.
# Sem nenhum dos dois o app sobe normalmente e a tela de pagamento avisa que a
# chave falta, em vez de exibir um texto qualquer para a pessoa copiar.
# Em dev vale o mesmo arquivo, em backend/.env (também ignorado pelo git).
JCARD_PIX_TIPO=CPF
JCARD_PIX_CHAVE=000.000.000-00
JCARD_PIX_TITULAR=Nome do titular

# ----------------------------------------------------------------- app --
JCARD_APP_URL=https://jcardapp.duckdns.org
# Rollback: aponte para um SHA já publicado no GHCR.
JCARD_IMAGE_TAG=latest
# Só mexa se trocar o tamanho da VM. O padrão (512m) foi medido na e2-micro.
# JCARD_MEM_BACKEND=512m

# --------------------------------------------------------------- e-mail --
# SMTP do próprio Gmail com senha de app: o e-mail sai assinado pelo Google e
# passa no DMARC. Relays de terceiros recusam remetente @gmail.com.
#   1) ative a verificação em 2 etapas na conta remetente
#   2) gere uma senha de app em https://myaccount.google.com/apppasswords
#   3) JCARD_MAIL_FROM tem de ser a MESMA conta que autentica no SMTP
JCARD_NOTIF_ENABLED=false
JCARD_MAIL_FROM=JcardApp <sua-conta@gmail.com>
JCARD_SMTP_HOST=smtp.gmail.com
JCARD_SMTP_PORT=587
JCARD_SMTP_USER=sua-conta@gmail.com
JCARD_SMTP_PASS=senha-de-app-de-16-letras

# ------------------------------------------------- parser da fatura (opcional) --
# Só preencha para calibrar um layout novo do Itaú sem precisar de deploy.
# Vazias = usa o padrão do application.properties. Ver docs/parser-itau.md.
# JCARD_PARSER_ITAU_PORTADOR=
# JCARD_PARSER_ITAU_LANCAMENTO=
# JCARD_PARSER_ITAU_TOTAL=
# JCARD_PARSER_ITAU_VENCIMENTO=
